{
  "name": "doover-node-red",
  "version": "0.1.0",
  "private": true,
  "description": "Doover x Node-RED monorepo: transport core (@doover/nodered-core) and the palette package (node-red-contrib-doover). Lives alongside the Python device app at the repo root.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "fuzz:differential": "node tools/differential/run.js --count 5000 --max-mismatch 0"
  },
  "engines": {
    "node": ">=24"
  },
  "license": "MIT"
}
